import React from "react";

// default to hamburger checkbox
// default hidden and show latest 10 blogs?
const TagSelector = (props) => {
  const { selectedTag } = props;
  return <div></div>;
};

export default TagSelector;
